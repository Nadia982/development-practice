// 1. Create a constant title with an initial value: 'devMentor'
// 2. Iterate through title letters and print one by one to the console
//(read documentation about string length and how to get a letter by index)

const title = "devMentor";

for (let i = 0; i < title.length; i++) {
  console.log(title[i]);
}
