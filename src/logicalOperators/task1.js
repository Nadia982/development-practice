// Define two variables: a, b

// Assign initial string values: empty string '', and zero '0'

// Print to the console expression: a || b

// Print to the console expression: a ?? b

// Guess the result and verify by executing the code

const a = "";
const b = "0";
console.log(a || b);
// I predict the result will be '0'

console.log(a ?? b);
// I predict the result will be ''
